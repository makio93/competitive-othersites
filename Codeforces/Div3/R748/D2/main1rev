自分の解では，配列Ai内のO(n)個についての全組合せO(n^2)についてgcdを計算する(O(log(Ai)))という処理を
Aiの各値についてO(n)回繰り返していたが，それだと全体でO(T*n^3*log(Ai))くらいの計算量になる．
それだと，10*40*40*40*log(10^6)==1.3e7くらいになる，これでも高速なので通る
想定解だと，gcdのかわりに各Aiの約数の値ごとの総数を集計してそれらのn/2-zcnt個以上あるものの最大値を
探す，という処理で同じような計算を実現しているが，それだとO(T*n^2*√Ai)くらいになるので速い．
