#include <bits/stdc++.h>
using namespace std;

using ll = long long;
class Solution {
public:
    ll kthSmallestProduct(vector<int>& nums1, vector<int>& nums2, ll k) {
        ll lval = (ll)(-1.1e10), rval = (1.1e10);
        while (lval <= rval) {
            
        }
    }
};
