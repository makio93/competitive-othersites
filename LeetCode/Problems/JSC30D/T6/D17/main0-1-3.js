// 自力WA1-3

/**
 * @param {any} o1
 * @param {any} o2
 * @return {boolean}
 */
var areDeeplyEqual = function(o1, o2) {
    const isEqual = (o1, o2)
    if (o1.toString() !== o2.toString()) return false;
    
}
